<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Birthday Aayu 🌸</title>

<style>
    body {
        margin: 0;
        height: 100vh;
        overflow: hidden;
        background: linear-gradient(135deg, #ff9a9e, #fad0c4);
        font-family: 'Segoe UI', sans-serif;
        color: white;
        text-align: center;
    }

    .container {
        position: relative;
        top: 50%;
        transform: translateY(-50%);
        z-index: 10;
        padding: 20px;
    }

    h1 {
        font-size: 3.5em;
        margin-bottom: 10px;
        animation: glow 2s infinite alternate;
    }

    h2 {
        font-size: 2em;
        margin-top: 0;
    }

    p {
        font-size: 1.2em;
        max-width: 600px;
        margin: 20px auto;
    }

    button {
        padding: 15px 35px;
        font-size: 1.1em;
        border: none;
        border-radius: 40px;
        background: #ff4d6d;
        color: white;
        cursor: pointer;
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        transition: 0.3s;
    }

    button:hover {
        background: #ff1e56;
        transform: scale(1.05);
    }

    .flower {
        position: absolute;
        font-size: 24px;
        animation: fall linear infinite;
    }

    @keyframes fall {
        0% {
            transform: translateY(-50px) rotate(0deg);
            opacity: 1;
        }
        100% {
            transform: translateY(110vh) rotate(360deg);
            opacity: 0;
        }
    }

    @keyframes glow {
        from {
            text-shadow: 0 0 10px #fff, 0 0 20px #ff6f91;
        }
        to {
            text-shadow: 0 0 20px #fff, 0 0 40px #ff1e56;
        }
    }

    .hidden {
        display: none;
    }

    .surprise {
        font-size: 1.6em;
        margin-top: 25px;
        animation: fadeIn 2s forwards;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: scale(0.8); }
        to { opacity: 1; transform: scale(1); }
    }
</style>
</head>

<body>
   </style>

</head>

<body>

<button id="playBtn"> Tap to Play "DIL tu jaan tu"

</button>

<audio id="bgMusic" loop>

<source src="Dil Tu Jaan Tu (Full Video) Gurnazar Ft. Kritika Yadav ｜ New Punjabi Viral Song ｜ Chet Singh.mp3" type="audio/mpeg">

</audio>

<script>

const btn = document.getElementById("playBtn");

const music = document.getElementById("bgMusic");

btn.addEventListener("click", () => {

    music.play()

    .then(() => {

        btn.style.display = "none"; // Hide button after play starts

        })

    .catch(err => {

    console.log("Autoplay blocked;", err);

    });

});

</script>

<div class="container">
    <h1>🎉 Happy Birthday Aayu 🎂</h1>
    <h2>My Best Friend 💖</h2>

    <p>
        Aayu, you make every moment brighter just by being you.
        Thank you for all the laughter, memories, and love.
        I’m so grateful to have you in my life 🌸
    </p>

    <button onclick="startFlowers()">Click for Flower Surprise 🌸</button>

    <div id="surprise" class="hidden surprise">
        🌷🌸 May your life bloom with happiness, love & success 🌸🌷<br>
        Always stay amazing 💕
    </div>
</div>

<script>
function startFlowers() {
    document.getElementById("surprise").classList.remove("hidden");

    const flowers = ["🌸","🌼","🌺","🌷","💐"];
    for (let i = 0; i < 40; i++) {
        let flower = document.createElement("div");
        flower.className = "flower";
        flower.innerHTML = flowers[Math.floor(Math.random() * flowers.length)];
        flower.style.left = Math.random() * 100 + "vw";
        flower.style.animationDuration = (Math.random() * 3 + 3) + "s";
        flower.style.fontSize = (Math.random() * 20 + 20) + "px";
        document.body.appendChild(flower);

        setTimeout(() => {
            flower.remove();
        }, 6000);
    }
}
</script>

</body>
</html>
